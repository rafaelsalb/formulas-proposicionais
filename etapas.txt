etapa 1 - análise léxica
	verificar se os símbolos contidos nas fórmulas inseridas são válidos
	símbolos válidos:
		Negação(∼∼), Conjunção(^), Disjunção(v), Condicional(→→), Bi-condicional(↔↔)
	caso de teste: ~A v {B → C ∧ [A ↔ B v (A)]}

etapa 2 - análise sintática
	verificar se a fórmula é FBF
		FBF:		(A → B) ∧ (B → A)
		Não-FBF:	A)) ∧∧ → BC
	quando é não-fbf?
		operador seguido de operador

etapa 3 - provador de tautologia
	avaliar fórmulas proposicionais (FBF) com o método dos tableaux

etapa 4 - formular apresentação
